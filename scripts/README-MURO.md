# Muro público MIRYAM 25

Hay un solo muro: `muro.json` + `muro-media/` en GitHub Pages.
Todo el mundo ve lo mismo. No hay muro por navegador.

La web envía la felicitación al bot. La automatización `muro-celebra-publicar` la copia a `muro.json` (sin pedir SÍ).
Hasta que corre esa tarea, el muro puede seguir vacío.
